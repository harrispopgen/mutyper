name: mutyper
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - python
  - bcftools
  - tabix
  - pip
  - pip:
    - -r requirements.txt
    - . # installs package from current directory
